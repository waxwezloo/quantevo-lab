# QuantEvo Lab — Python-движок
# Скопируйте в .env и заполните. Ключи: Bybit → API Management.
# Для экспериментов используйте https://testnet.bybit.com (отдельные ключи!).

BYBIT_API_KEY=
BYBIT_API_SECRET=

# Необязательные значения по умолчанию (переопределяются флагами CLI)
SYMBOL=BTCUSDT
TIMEFRAME_MIN=60
MARKET_MODE=spot
